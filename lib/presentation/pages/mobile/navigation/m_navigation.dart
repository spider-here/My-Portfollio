import 'package:flutter/material.dart';

class MNavigation extends StatelessWidget{
  const MNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }

}